---
const currentYear = new Date().getFullYear();
---

<footer class="fixed bottom-8 left-1/2 -translate-x-1/2 z-50">
    <div class="relative">
        <div class="absolute inset-0 bg-gray-900/80 backdrop-blur-xl rounded-2xl border border-gray-800/50"></div>
        <div class="relative px-8 py-1">
            <div class="flex items-center gap-6">
                <div class="text-sm text-gray-400">
                    <span>(C) {currentYear} Eastern Shore IT</span>
                    <span class="mx-2">|</span>
                    <a href="/privacy" class="hover:text-white transition-colors">Privacy Policy</a>
                    <span class="mx-2">|</span>
                    <a href="/terms" class="hover:text-white transition-colors">Terms of Service</a>
                </div>
            </div>
        </div>
    </div>
</footer>

<style>
    @keyframes scaleUp {
        0% { transform: scale(0.8); opacity: 0; }
        100% { transform: scale(1); opacity: 1; }
    }

    .scale-up {
        animation: scaleUp 0.2s ease-out forwards;
    }
</style>
